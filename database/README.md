# Banco de dados

O SQL inicial do MVP está em:

`src/painel/painel-bora-pra-obra/install.sql`

Futuras alterações devem ser criadas em `database/migrations/` com nomes versionados, por exemplo:

`001_create_costs_table.sql`

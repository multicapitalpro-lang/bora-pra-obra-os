<?php require __DIR__.'/includes/auth.php';require __DIR__.'/config/database.php';$pageTitle='Temporadas';$rows=db()->query('SELECT t.*,COUNT(e.id) total FROM temporadas t LEFT JOIN episodios e ON e.temporada_id=t.id GROUP BY t.id ORDER BY t.ordem')->fetchAll();require __DIR__.'/includes/header.php';?>
<div class="row g-3"><?php foreach($rows as $r): ?><div class="col-md-6 col-xl-4"><div class="panel-card h-100"><div class="small text-secondary">Temporada <?= (int)$r['ordem'] ?></div><h2 class="h5 mt-1"><?= htmlspecialchars($r['nome']) ?></h2><p class="text-secondary mb-4"><?= htmlspecialchars($r['objetivo']) ?></p><div class="d-flex justify-content-between"><span class="badge text-bg-light border"><?= (int)$r['total'] ?> capítulos</span><span class="small text-secondary"><?= htmlspecialchars($r['faixa_capitulos']) ?></span></div></div></div><?php endforeach; ?></div>
<?php require __DIR__.'/includes/footer.php';?>
